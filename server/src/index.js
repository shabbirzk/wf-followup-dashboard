require('dotenv').config();const express=require('express');const cors=require('cors');const mongoose=require('mongoose');const app=express();app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:3000'}));app.use(express.json());app.use('/api',require('./routes/api'));mongoose.connect(process.env.MONGODB_URI||'mongodb://127.0.0.1:27017/wfFollowupDB').then(()=>app.listen(process.env.PORT||5000,()=>console.log('API running on port '+(process.env.PORT||5000)))).catch(e=>{console.error(e);process.exit(1)});
